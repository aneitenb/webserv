# error pages and client body size
server {
    listen 8080;
    host 127.0.0.1;
    server_name localhost;
    root www;
    index index.html;
    client_max_body_size 1024;
    allowed_methods GET POST;
    error_page 404 /default_errors/404.html;
}

# test custom 404 page
#curl -v http://127.0.0.1:8080/does-not-exist.html

# test body size limit (create file larger than 1024 bytes)
#dd if=/dev/zero of=large_data.txt bs=2048 count=1
#curl -v -X POST http://127.0.0.1:8080/test.txt --data-binary @large_data.txt

# test forbidden (if you can create a file without read permissions)
#chmod 000 www/test.txt
#curl -v http://127.0.0.1:8080/test.txt
#chmod 644 www/test.txt  # restore permission